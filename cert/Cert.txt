docker run -it --rm \
  -p 80:80 \
  -v "$(pwd)/certbot/etc:/etc/letsencrypt" \
  -v "$(pwd)/certbot/var:/var/lib/letsencrypt" \
  certbot/certbot certonly \
  --standalone \
  -d peremenacoffee.ru -d petr-prozorov.ru
